const authController = require('./authController')
const analisisController = require('./analisisController')
const uploadController = require('./uploadController')
const filtrosController = require('./filtrosController')

module.exports = {
    authController,
    analisisController,
    uploadController,
    filtrosController
}