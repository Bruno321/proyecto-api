const authRoutes = require("./authRoutes")
const analisisRoutes = require('./analisisRoutes')
const uploadRoutes = require('./uploadRoutes')
const filtroRoutes = require('./filtroRoutes')

module.exports = {
    authRoutes,
    analisisRoutes,
    uploadRoutes,
    filtroRoutes
}